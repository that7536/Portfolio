<div class='container'>
  <div>
    <h3>【投稿の検索結果】検索ワード：<%= @content %></h3>
    <table>
      <thead>
        <tr>
          <th>投稿者名</th>
          <th>タイトル</th>
        </tr>
      </thead>
      <tbody>
        <% @records.each do |record| %>
          <tr class='tag-lists-box'>
            <td class='tag-lists'>
              <%= link_to record.user.name, user_path(record.user.id) %><br>
              <%= attachment_image_tag(record.user, :profileimage, :size =>'100x100', fallback: "no-image-icon.jpg") %>
            </td>
            <td class='tag-lists'><%= link_to record.title, text_path(record) %></td>
          </tr>
        <% end %>
        <%= paginate @records %>
      </tbody>
    </table>
    <h2>タグ一覧(新着順)</h2>
    <table>
      <tr>
        <t><%= link_to "  投稿一覧表示", tags_path(search: "")%><br></t>
        <% @tags.reverse.each do |tag| %>
          <t>
            <%= link_to tags_path(tag_id: tag) do %>
              <%= tag.name %><br>
            <% end %>
          </t>
        <% end %>
        <%= paginate @tags %>
      </tr>
    </table>
  </div>
</div>