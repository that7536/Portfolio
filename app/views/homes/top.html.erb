<h1>The Old Gang</h1>

<h4>
  The Old Gangは古いアーケードゲームを専門に扱う情報共有サイトです。</br>
  個人・商業の方など、どなたでもご利用いただけます。
</h4>
<% if user_signed_in? %>
  <h2>最新の投稿</h2>
  <% @texts.last(4).reverse.each do |text| %>
    <tr id="text_<%= text.id %>">
      <td>
        <%= link_to text_path(text.id) do %>
          <%= attachment_image_tag(text, :image, :size =>'400x300', fallback: "no-image-icon.jpg") %>
        <% end %>
      </td>
      <td>
        <%= link_to user_path(text.user.id) do %>
          <%= attachment_image_tag(text.user, :profileimage, :size =>'100x100', fallback: "no-image-icon.jpg") %>
        <% end %>
      </td>
      <td><%= link_to text.title, text_path(text), class: "text_#{text.id}" %></td>
      <td><%= text.comments.count %>件のコメント</td>
      <td>
        <!--ブックマーク登録・解除-->
          <% if current_user.bookmarks.where(text_id:text.id).present? %>
        <%# binding.pry %>
            <%= link_to "ブックマークに保存済み", text_bookmark_path(text.id,text.user_id ), method: :delete %>
          <% else %>
            <%= link_to "ブックマークに保存", text_bookmarks_path(text.id), method: :post %>
          <% end %>
      </td>
    </tr>
  <% end %>
<% else %>
  <h1><%= link_to '新規登録', new_user_registration_path,class: 'fas fa-user-plus nav-link text-light' %></h1>
  <h1><%= link_to 'ログイン', new_user_session_path ,class: 'fas fa-sign-in-alt nav-link text-light'%></h1>
<% end %>
