<!DOCTYPE html>
<html>
  <head>
    <title>TheOldGang</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/"><span>The Old Gang</span></a>
          <!--<button>-->
            <!--<span class="navbar-toggler-icon"></span>-->
          <!--</button>-->
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li><%= link_to 'マイページへ',user_path(current_user),class: 'fas fa-home nav-link text-light' %></li>
              <li><%= link_to '新規投稿',new_text_path,class: 'fas fa-home nav-link text-light' %></li>
              <li><%= link_to 'ブックマーク',bookmarks_path,class: 'fas fa-home nav-link text-light' %></li>
              <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete,class: 'fas fa-sign-out-alt nav-link text-light' %></li>
              <li><%= render 'searchs/form' %></li>
            <% else %>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <%= yield %>
  </body>
</html>
